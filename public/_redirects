# Cloudflare Pages redirects and rewrites for /quakeweather/ subdirectory
# Redirect /quakeweather to /quakeweather/
/quakeweather /quakeweather/ 301

# Serve index.html for /quakeweather/ (the HTML already has /quakeweather/ paths)
/quakeweather/ /index.html 200

# Rewrite asset requests: /quakeweather/assets/* -> /assets/* (remove base path)
# This is needed because assets are deployed at root /assets/, not /quakeweather/assets/
/quakeweather/assets/* /assets/:splat 200
/quakeweather/models/* /models/:splat 200  
/quakeweather/favicon.svg /favicon.svg 200

